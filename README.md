mmd: Monospaced Markdown
---

A markdown flavor to preserve the look and feel of the source files.

### Getting started

Install with:

```shell
pipx install 
```

And run with:

```shell
mmd html input.mmd --open
```
